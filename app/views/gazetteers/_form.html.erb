<%= simple_form_for @gazetteer do |f| %>
  <% if @gazetteer.errors.any? %>
    <div class="panel">
      <h3><%= pluralize(@gazetteer.errors.count, "error") %> prohibited this gazetteer from being saved!</h3>
      <% @gazetteer.errors.full_messages.each do |msg| %>
        <p><%= msg %></p>
      <% end %>
</div>
  <% end %>
<div class="row">
    <div class="large-12 columns">
      <%= f.input :category_id, collection: Category.all, prompt: "Select your gazetteer category" %>
      <%= f.input :address, label: 'address' %>
      <%= f.button :submit, label: 'Create gazetteer' %>
    </div>
</div>
<% end %>