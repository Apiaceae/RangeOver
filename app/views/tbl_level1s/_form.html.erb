<%= form_for(@tbl_level1) do |f| %>
  <% if @tbl_level1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tbl_level1.errors.count, "error") %> prohibited this tbl_level1 from being saved:</h2>

      <ul>
      <% @tbl_level1.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :continent %><br>
    <%= f.text_field :continent %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
